# 📌 Tuple Practice Questions
# Beginner Level
# Create a tuple of 5 numbers and print the first and last element.
# Convert a tuple to a list, change one element, and convert it back to a tuple.
# Check if "Python" exists in ("Java", "C", "Python").
# Intermediate Level
# Count the number of occurrences of 3 in (1, 3, 3, 3, 5).
# Find the index of 20 in (10, 20, 30, 40).
# Unpack (100, 200, 300) into three variables.
# Advanced Level
# Write a function that returns a tuple (sum, product) of two numbers.
# Merge two tuples and sort them.
# Convert a list of tuples [(1, 2), (3, 4), (5, 6)] into a dictionary {1: 2, 3: 4, 5: 6}.


fruits = ("apple", "banana", "cherry")

 
print(fruits[2])   
print(fruits[-2])  
print(fruits[-1]) 
print(fruits[+2]) 