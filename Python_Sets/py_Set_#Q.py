# 📌 Practice Questions
# Beginner Level
# Create a set of 5 fruits and print it.
# Add "grape" to the set {"apple", "banana", "cherry"}.
# Remove "banana" from a set using discard().
# Intermediate Level
# Find the common elements of {1, 2, 3} and {3, 4, 5}.
# Find the difference between {10, 20, 30} and {20, 40, 50}.
# Check if {2, 3} is a subset of {1, 2, 3, 4}.
# Advanced Level
# Merge two sets without using union().
# Write a function that removes duplicate elements from a list using a set.
# Convert a set to a frozenset and check if modification is possible.